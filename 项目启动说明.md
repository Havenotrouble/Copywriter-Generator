# 项目启动说明

## 📂 位置
所有启动/停止脚本都在项目根目录：
```
D:\liziyi\codeprivate\Copywriter-Generator\
```

## 🚀 启动项目

### 方法一：一键启动（推荐）
双击运行根目录下的 **`start-all.bat`**

### 方法二：手动启动
1. **启动后端**：
   - 双击 `backend-api/start-backend.bat`
   - 或在根目录执行：`cd backend-api && start-backend.bat`
   - 端口：8080

2. **启动前端**：
   - 在根目录执行：`cd frontend-web && npm run dev`
   - 端口：3000

## 🛑 停止项目

### 方法一：一键停止（推荐）
双击运行根目录下的 **`stop-all.bat`**

### 方法二：手动停止
关闭命令行窗口，或执行以下命令：

**停止前端（3000端口）：**
```bash
netstat -ano | findstr ":3000 " | findstr LISTENING
taskkill /F /PID <进程ID>
```

**停止后端（8080端口）：**
```bash
netstat -ano | findstr ":8080 " | findstr LISTENING
taskkill /F /PID <进程ID>
```

## 🌐 访问地址

- **前端页面**：http://localhost:3000
- **后端API**：http://localhost:8080
- **健康检查**：http://localhost:8080/api/health

## ⚠️ 常见问题

### 端口被占用
运行 `stop-all.bat` 停止所有服务后重新启动

### 前端无法访问后端
确认后端服务已启动（访问 http://localhost:8080/api/health 应返回 "OK"）

### 修改代码后不生效
- **后端**：需要重启服务（stop-all.bat → start-all.bat）
- **前端**：Next.js 会自动热更新，无需重启

## 📝 开发模式

如果需要查看详细日志，建议手动启动：

1. 打开两个命令行窗口
2. 窗口1：`cd backend-api && start-backend.bat`
3. 窗口2：`cd frontend-web && npm run dev`

这样可以同时看到前后端的实时日志输出。
